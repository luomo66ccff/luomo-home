# Security Policy

Luomo Home 本身不应保存账号密码。它会从服务端读取健康探针，并可连接角色对话 API，因此部署时仍需注意：

- 所有后端 URL 使用 HTTPS 或受信任的容器网络；
- `NEXT_PUBLIC_*` 变量绝不放秘密；
- 状态代理设置短超时，避免不可用服务拖住页面渲染；
- 角色对话后端自行完成鉴权、内容限制与速率限制；
- 私人服务入口应继续由 Access 或自身登录保护，首页链接不是授权层。

发现服务端请求伪造、秘密进入客户端包、跨站脚本或对话接口越权，请通过 GitHub 私密安全公告报告。

